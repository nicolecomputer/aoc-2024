// import { describe, it } from "https://deno.land/x/test_suite@0.16.1/mod.ts";

// import { splitLines } from "./util.ts";

// describe("splitLines", () => {
//     it("returns an array of lines", () => {
//         expect(splitLines(
//             `
// A
// B
// C
// `
//         )).toEqual(['', 'A', 'B', 'C', ''])
//     })

//     it("supports trimming an array of lines", () => {
//         expect(splitLines(
//             `
// A
// B
// C
// `, { trim: true }
//         )).toEqual(['A', 'B', 'C'])
//     })
// })
